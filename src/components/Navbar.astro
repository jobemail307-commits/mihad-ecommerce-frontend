---
import { ShoppingBag, User } from "@lucide/astro";
import MenuButton from "@/components/MenuButton";
import { links } from "@/lib/constants";
import Wrapper from "@/components/NavbarWrapper";
---

<Wrapper client:load>
  <nav
    class="grid w-full wrapper grid-cols-[1fr_min-content_1fr] py-6 px-8 z-2 items-center"
  >
    <div>
      <ul class="hidden lg:flex gap-3 text-sm items-center">
        {
          links.map((link) => (
            <li>
              <a href={link.href}>{link.name}</a>
            </li>
          ))
        }
      </ul>
      <div class="lg:hidden">
        <MenuButton client:load />
      </div>
    </div>
    <a
      href="/"
      class="font-heading group-data-[top=true]:text-4xl text-2xl transition-all duration-500"
      >Logo</a
    >
    <div class="flex flex-row-reverse gap-3">
      <a href="/cart" aria-label="Shopping cart">
        <ShoppingBag />
      </a>
      <a href="/login" aria-label="Log in">
        <User />
      </a>
    </div>
  </nav>
</Wrapper>
